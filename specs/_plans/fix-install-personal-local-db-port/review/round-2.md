# Plan Review Findings: fix-install-personal-local-db-port (round 2)

## Summary
- Axes checked: 6/6
- Total findings: 2 (Blockers: 0, Advisory: 2)
- Intent Fidelity blockers: 0

Counts cover findings raised in **this** round. Round 1's seven advisories were deliberately not addressed (advisories are report-only) and are listed below as carried forward, not re-counted.

## Round-1 Blocker Recheck

- **Resolved:** [TRACEABILITY_GAP] task 1(c)'s `8563` assertion was vacuous — the fix is present in all three places the round-1 `Fix:` named, and each was re-read against the current artifacts:
  - plan.md § Implementation Tasks task 1 (line 86) now states the group rule: "calls `reset_connection_globals`, sets the sentinel `PORT="unresolved"`, then runs `resolve_local_connection "$dir"`" and "All three tests MUST set that sentinel, re-setting it before every `resolve_local_connection` call". It gives the reason inline and points at the harness line that made the old assertion vacuous (`reset_connection_globals` leaves `PORT=8563`).
  - 1(a) re-sets the sentinel between its two resolutions: "asserts return `0`, `HOST` is `127.0.0.1` and `PORT` is `52164`; then re-set the sentinel, rewrite the same descriptor with `"dbPort":59446`, resolve again, and assert `PORT` is `59446`". The "read fresh, not cached" pair is therefore falsifiable in both halves.
  - 1(b) carries the sentinel with `CLI_PORT="1234"` pre-set; 1(c) carries it and the plan states the consequence explicitly: "Here the sentinel is what makes the assertion falsifiable: it proves `resolve_local_connection` applied the `deployment_field` default instead of leaving the `8563` that `reset_connection_globals` wrote."
  - plan.md § Verification § Scenario Coverage (lines 123-124) carries the required caveat verbatim in substance: "The local scenario's default-fallback clause … counts as covered only because task 1(c) sets the `PORT="unresolved"` sentinel before resolving. Drop the sentinel and `reset_connection_globals`' own `PORT=8563` satisfies the assertion, making this row's coverage claim false."
  - decision-log.md § Review Findings records the change under `### [plan-review] Task 1(c)'s 8563 assertion was vacuous (round 1 BLOCKER)` with an accurate Direction change and no scope movement ("Test scope is unchanged — three tests, same fixtures, same expected values").
  - Independently verified against the real files, not the plan's summary: `reset_connection_globals` sets `PORT=8563` at `scripts/tests/install-personal-test.sh:126` and `CLI_PORT=""` at `:128`; `HOST=""` at `:125`, so 1(a)'s `HOST` assertion needs no sentinel; the vacuous model test sits at `:251-269`. The sentinel cannot leak into the cloud tests — every one of them calls `reset_connection_globals` first (`:169`, `:195`, `:221`, `:241`, `:259`, `:279`), and the two intervening tests (`:105`, `:135`) read neither `PORT` nor `CLI_PORT`.
- **Carried forward, unaddressed by design** (round-1 advisories, not re-litigated here): [UNSTATED_ASSUMPTION] manual-matrix premise unvalidatable by the Checklist; [HIDDEN_DEPENDENCY] CLAUDE.md version-bump rule unresolved; [AMBIGUOUS_REQUIREMENT] "only when the field is absent"; [COMPLETENESS_GAP] no `--host` clause in the local scenario; [COMPLETENESS_GAP] task 3 misses `scripts/install.sh:396-398` and `:50-51`; [TASK_GRANULARITY] rename split from its `usage()` call site; [TACTICAL_SHORTCUT] three unscheduled deferrals; [PROSE_BLOAT] reviewer-facing passages. The spec delta is byte-identical to round 1 (`diff` against `specs/container/personal-install/spec.md` shows only the four DELTA blocks), so none of these was disturbed by the blocker fix.

## Premortem

Three fresh failure stories for the revised plan, each routed into the taxonomy below.

1. **The cloud twin stays blind.** The sentinel idiom lands in the three new local tests, and `resolves_cloud_defaults_when_connection_fields_absent` keeps asserting the two values `reset_connection_globals` already wrote. A later edit drops `resolve_cloud_connection`'s `'8563'`/`'sys'` default arguments (`scripts/install.sh:240-241`); the suite stays green; a cloud Personal install builds a DSN with an empty port and user. The plan named this hole and stepped around it. → Advisory, Task Breakdown.
2. **The sentinel gets cleaned up.** A maintainer tidying the harness sees `PORT="unresolved"` three lines below `reset_connection_globals`, reads it as redundant setup, and deletes it. The `8563` assertion still passes, and the default-fallback path is unguarded again — the exact round-1 defect, reintroduced. The only defense is the rationale prose, whose over-long sentences are the first thing a skimmer drops. → Advisory, Prose Quality.
3. **`--help` dies mid-plan** (round-1 advisory, still open): task 2 renames `PERSONAL_DB_PORT`, task 3 fixes its only other reference at `scripts/install.sh:91`, and between the two groups `scripts/install.sh --help` aborts under `set -u`. Final state is consistent; only the intermediate commit breaks. → carried forward, not re-raised.

## Intent Fidelity

No objection — axis checked. Issue #83's body was re-fetched (`gh issue view 83`) and every scoped element still maps: the hardcode at `scripts/install.sh:41`/`:401`, the host-side forwarder mechanism, the reproduction's own ports (`default`:8563, `agent-alpha`:52164, `agent-beta`:59446 — the values task 1 uses), `.connection.dbPort` as the field, the silently-discarded `--port`, and the spec defect at `specs/container/personal-install/spec.md:9`. The four interview answers are each honored: core-fix-only scope, `--port` honored for local via `CLI_PORT` (decision [3]), a minimal extraction reusing `deployment_field` and drivable from the sourced harness (decision [1]), corrected background plus a local scenario mirroring the cloud one (delta lines 10, 68-76). The banner print stays out (decision [8]) as the interview required. The round-2 revision moved no scope: it changed test-assertion mechanics only, and decision-log.md § Review Findings says so ("Test scope is unchanged").

## Feasibility

No objection — axis checked against the current files, not the plan's line references. `deployment_field` (`scripts/install.sh:192-207`) substitutes the default when the field is absent **or** empty and returns 1 on an unreadable descriptor, which is what makes 1(a)'s third assertion (`rm -f "$dir/deployment.json"` → return `1`) reachable and makes task 2's `|| return 1` load-bearing: `resolve_cloud_connection` uses `|| true` (`:239-241`) because its `HOST` presence check catches a missing descriptor, whereas the local function has no such check, so `|| true` there would yield `PORT=""` and a `127.0.0.1:` DSN. Every other cited line is correct as written: `PERSONAL_DB_PORT` occurs at exactly `:41`, `:91`, `:401` (grep, whole repo); the `local x="$(…)"` masking comment at `:237-238`; `resolve_cloud_connection`'s close at `:265`; the `CLI_PORT` capture at `:349`; the two hardcode lines at `:400-401`; the transport comment at `:375-377`; `docs/installation.md` lines 57, 81, 118, 120 verbatim. The harness runs `set -uo pipefail` (`:5`) with `set +e` after sourcing (`:21`), so a sentinel-carrying test and a `return 1` both behave as the plan assumes, and a missed rename would surface as an unbound-variable failure inside the command substitution rather than a silent pass. Carried-forward round-1 advisories on this axis (manual-matrix premise, version-bump rule) are unchanged.

## Requirement Quality

No objection — axis checked. The delta is unchanged from round 1: `diff` against the recorded `specs/container/personal-install/spec.md` yields only the three `DELTA:CHANGED` blocks and one `DELTA:NEW` block, each wrapping exactly the paragraph or scenario it claims. `speq feature validate container/personal-install` reports 0 errors / 0 warnings. All four clauses of the new local scenario have implementing assertions after the fix: `127.0.0.1` host and descriptor port in 1(a), the default fallback in 1(c) (now falsifiable), the `--port` override in 1(b), and the unreadable-descriptor failure in 1(a)'s third resolution. The two carried-forward round-1 advisories on this axis (the word "only" in delta line 73, and the absent `--host` clause) are untouched by the blocker fix.

## Task Breakdown

#### [COMPLETENESS_GAP] ADVISORY
- Location: plan.md § Implementation Tasks task 1; `scripts/tests/install-personal-test.sh:251-269`
- Issue: the revision fixes the vacuous assertion in the three new tests and leaves the same defect in the model test standing next to them. Task 1 now says of `resolves_cloud_defaults_when_connection_fields_absent`: "That is the vacuous shape of the model test … copy its structure, not its unguarded assertion." That test calls `reset_connection_globals` (`:259`), which writes `PORT=8563` (`:126`) and `USER=sys` (`:127`), then asserts `"8563" "$PORT"` (`:265`) and `"sys" "$USER"` (`:266`) — both assertions pass even if `resolve_cloud_connection` never assigns either global. So after this plan the harness holds two sentinel-guarded local defaults and two unguarded cloud defaults, and the next author copying the nearest cloud model reproduces the hole a third time. Dropping the `'8563'`/`'sys'` default arguments at `scripts/install.sh:240-241` would leave the suite green; the plan's own § Verification § Scenario Coverage counts those cloud rows as "covered as recorded".
- Fix: In plan.md § Implementation Tasks task 1, add a sub-item (d): in the existing `resolves_cloud_defaults_when_connection_fields_absent`, set `PORT="unresolved"` and `USER="unresolved"` immediately after the `reset_connection_globals` call (`scripts/tests/install-personal-test.sh:259`) so its `8563` and `sys` assertions (`:265-266`) become falsifiable, and note that no other cloud test needs it because their expected values differ from the reset values. If the planner judges that edit out of scope, instead add a decision to decision-log.md § Design Decisions stating that the cloud defaults test keeps its unguarded assertions, and name the `feature`-labelled GitHub issue that will fix it — per CLAUDE.md § Specs & issues, an issue is this project's only schedule.

Otherwise no objection — axis checked. The three new tests are registered in the `run` list after `reads_ssh_port_from_deployment_json` (`:298`), and definition order is irrelevant because every `run` invocation follows all definitions (`:296-306`). No sentinel or `CLI_PORT` can leak forward: the two tests that follow the insertion point read neither global, and every cloud test resets first. Each delta block still maps to a task and no task implements anything outside the delta. The Group A → B → C ordering and its stated dependencies are unchanged and still correct.

## Design Depth

No objection — axis checked per `/speq:design-philosophy`'s Quick Diagnostic. The round-2 revision touched no interface, module, or boundary: `resolve_local_connection`'s shape, its global-assigning seam, and its `deployment_field` dependency are identical to round 1. One sentence still covers its responsibility; after the change exactly one place owns "where a local deployment's SQL endpoint is", replacing a split between a file-level constant and two lines of `main`; routing `.connection.dbPort` through `deployment_field` closes a back-door leakage in which one descriptor field had two readers. The carried-forward [TACTICAL_SHORTCUT] advisory about the three unscheduled deferrals is unchanged.

## Prose Quality

#### [PROSE_BLOAT] ADVISORY
- Location: decision-log.md § Review Findings → `### [plan-review] Task 1(c)'s 8563 assertion was vacuous (round 1 BLOCKER)` (Finding and Direction-change bullets)
- Issue: the newly added entry is governed prose (decision-log Finding and Rationale prose) and breaches the 25-word sentence cap in the two sentences that carry the fix's reasoning. "Task 1 now requires the sentinel `PORT="unresolved"` in all three tests, set after `reset_connection_globals` and re-set before every `resolve_local_connection` call, and states that (c)'s assertion is falsifiable only because of it." is 31 words and chains three claims. "Tasks 1(a) and 1(b) carry the sentinel for uniformity: their expected values (`52164`, `59446`, `1234`) already differ from the reset value, so no assertion in the group depends on what `reset_connection_globals` writes." is 32 words. A third sentence buries its subject: "The § Scenario Coverage row therefore claimed coverage for the default-fallback clause — the no-change path for every single-deployment operator — that no assertion could break", where the relative clause sits 12 words from its antecedent. This entry is the only durable record of *why* the sentinel exists; a maintainer who skims it deletes the sentinel as redundant setup and restores the round-1 defect.
- Fix: In decision-log.md, split the Direction-change bullet into three sentences, each under 25 words: (1) "Task 1 now requires the sentinel `PORT="unresolved"` in all three tests."; (2) "Each test sets it after `reset_connection_globals` and re-sets it before every `resolve_local_connection` call."; (3) "Only that sentinel makes 1(c)'s `8563` assertion falsifiable." Split the uniformity sentence into "Tasks 1(a) and 1(b) carry the sentinel for uniformity." plus "Their expected values (`52164`, `59446`, `1234`) already differ from the reset value." In the Finding bullet, rewrite the third sentence as "The § Scenario Coverage row therefore claimed unbreakable coverage for the default-fallback clause, which is every single-deployment operator's no-change path."

Otherwise no objection — axis checked. § Verification's new caveat (plan.md lines 123-124) is front-loaded, active, two sentences, both inside the cap, and it names the mechanism rather than the reviewer. § Summary, § Impact, Non-Goals, and the tables are unchanged from round 1. No banned filler (`basically`, `simply`, `obviously`, `just`, `actually`) and no escape clauses (`as appropriate`, `where possible`, `etc.`, `and/or`) appear in the added prose. Task 1's long paragraph is § Implementation Tasks prose, which `/speq:writing-guardrails` does not govern, so its length is not a finding — but see the carried-forward round-1 [PROSE_BLOAT] advisory for the governed passages that remain.
