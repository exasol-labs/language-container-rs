# Tasks: fix-slc-metadata-schema

## Phase 2: Implementation (Group A)
- [x] 2.1 Add `dist/tests/language_definitions_test.sh`, a plain-assertion contract script taking the document path as its argument, asserting `schema_version: 2`, root key `language_definitions` (not `languages`), one conforming definition (`aliases`, `protocol`, `parameters` as `{key, value}`, `udf_client_path.executable`, `deprecation` presence+null asserted separately), and absence of `arguments`. Run it against the unchanged `build_info/language_definitions.json` and confirm it fails on the root key.

## Phase 2: Implementation (Group B)
- [x] 2.2 Add one-per-defect negative fixtures under `dist/tests/fixtures/language_definitions/` (root key renamed, `deprecation` removed, `arguments` re-added, `parameters` re-typed, `executable` changed) plus `conforming.json`, and `dist/tests/language_definitions_fixtures_test.sh` driving task 2.1's script against each, asserting non-zero exit on the five negatives and exit 0 on `conforming.json`.
- [x] 2.3 Correct `build_info/language_definitions.json` per plan.md's Migration table (root key `language_definitions`, `parameters` k/v objects, `deprecation: null`, retain `language_identifier`). Re-run task 2.1's script against it and confirm all assertions pass.
- [x] 2.4 Add a `Run language definitions contract tests` step to the `unit-tests` job in `.github/workflows/ci.yml`, beside the existing `about.toml`/`install-script` test steps, invoking `dist/tests/language_definitions_test.sh build_info/language_definitions.json` then `dist/tests/language_definitions_fixtures_test.sh`.

## Phase 2: Implementation (Group C)
- [x] 2.5 Rewrite `slc_tarball_language_definitions_well_formed` in `dist/tests/slc_tarball_test.sh`: delete the substring `expected`/`compact` check, delegate to `bash "$HERE/language_definitions_test.sh" "$TREE/build_info/language_definitions.json"`, add a `cmp` proving the shipped copy is byte-identical to the committed source, and keep the executable-resolves cross-check.

## Phase 3: Verification
- [x] 3.1 Run automated checks (build, test, shell contract tests, integration, lint, format) per plan.md § Verification § Checklist
- [x] 3.2 Scenario coverage audit against plan.md § Verification § Scenario Coverage
- [x] 3.3 Manual verification per plan.md § Verification § Manual Testing (Docker tarball build, extraction — Personal custom-SLC install deferred: no live Exasol Personal deployment in this environment, flagged in verification-report.md as a pre-merge human gate)

## Phase 4: Review Fixes
- [x] 4.1 In `dist/tests/slc_tarball_test.sh`, add `command -v jq >/dev/null 2>&1 || die "jq not found — install jq"` immediately after the existing `command -v readelf ... || die "readelf not found — install binutils"` guard line, matching that guard's style, so a missing `jq` stops the run with its own named error before any assertion runs.
- [x] 4.2 In `dist/tests/language_definitions_fixtures_test.sh`, give `assert_rejected` a third parameter `expected_message` and rewrite it to capture the child's real status and output (`local` declared separately from the assignment, never `local output="$(...)"`): `die` when the contract script could not run (status 2), `fail` when status is 0 (fixture unexpectedly passed), `fail` with the captured output when status is 1 but no `FAIL:` line contains `$expected_message`, `pass` otherwise. Apply the same status-capture and die-on-2 to `assert_accepted`, keeping its `status != 0` failure. Pass the verified expected substring at every call site. Verify with a full run plus negative evidence from a deliberately wrong `expected_message`. [expert]
- [x] 4.3 Add six negative fixtures under `dist/tests/fixtures/language_definitions/`, each a copy of `conforming.json` varying exactly one fact — `schema_version_wrong.json`, `definitions_not_array.json`, `two_definitions.json`, `aliases_changed.json`, `protocol_changed.json`, `deprecation_non_null.json` — covering the six contract-script failure branches that have no fixture, and add one three-argument `assert_rejected` driver case per fixture with the expected `FAIL:` substring for the branch it targets. [expert]
- [x] 4.4 Delete the six wrapper function definitions in `dist/tests/language_definitions_fixtures_test.sh` whose whole body repeats their own name as a literal argument, and replace their bare invocations with direct `assert_rejected` / `assert_accepted` calls carrying the same test names, one call per line in a single fixture-to-name block. [expert]
- [x] 4.5 In `dist/tests/language_definitions_test.sh`, add a `language_definitions_document_is_valid_json` assertion that fails with `"$DOC is not valid JSON"` when `jq empty` is non-zero, call it first in the runner, and gate the four existing calls behind `[[ "$failures" -eq 0 ]]` so a malformed document reports one accurate failure instead of four misattributed ones. Add `dist/tests/fixtures/language_definitions/malformed.json` (trailing comma) and a `language_definitions_rejects_malformed` driver case expecting `is not valid JSON`. [expert]
