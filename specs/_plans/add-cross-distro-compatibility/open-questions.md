# Open Questions: add-cross-distro-compatibility

speq-plan-pr could not complete this plan without human input. What's done so far is committed on this branch. Reply inline on the PR, or resume with `/speq:plan add-cross-distro-compatibility` locally, or re-run `/speq:plan-pr add-cross-distro-compatibility` after commenting.

- [ ] Exasol's published "supported Linux" list (Ubuntu 20.04/22.04/24.04 LTS and RHEL 8/9/10 on the database-host page; openSUSE 15 and Debian 11/12 on the driver pages; Fedora on none of them) describes database hosts, not UDF-authoring hosts. Since the SLC ships its own root filesystem, a database host's distribution cannot reach inside it. The plan reads "the Rust SLC must be compatible with all Linux supported by Exasol" as "a Rust UDF author can build on every one of those distributions plus Fedora", served through `cargo exasol-udf build --container` wherever a host's glibc or rustc identity does not fit the plain build path. Is that the intended scope, or did you mean the SLC binary/runtime itself must run correctly against an Exasol database installed on each of those distributions, which is a different and larger scope this plan does not cover?
