# Plan Review Findings: fix-install-scope-registration (round 2)

## Summary
- Axes checked: 6/6
- Total findings: 2 (Blockers: 1, Advisory: 1)
- Intent Fidelity blockers: 0

## Round-1 Blocker Recheck

- Resolved: [REQUIREMENT_CONFLICT] The mission still stated `ALTER SESSION` registration — plan.md task 5 (`:48`) changes `specs/mission.md:22` and `:31` to `ALTER SYSTEM SET SCRIPT_LANGUAGES` and restates the glossary row at `:43` as the general Exasol mechanism. Both target lines verified in the live file: `:22` reads "Build + upload the container, `ALTER SESSION SET SCRIPT_LANGUAGES`, create scripts" and `:31` reads "registered with `ALTER SESSION SET SCRIPT_LANGUAGES`". A grep of `specs/mission.md` for `ALTER` and `SCRIPT_LANGUAGES` returns exactly those three lines, so task 5 covers every site. The glossary restatement is accurate: Exasol accepts both `ALTER SESSION` and `ALTER SYSTEM`, and the row defines the SLC concept, not the install script, so it introduces no new conflict with the delta clause "The install script exposes no scope option". Task 5 sits in Parallelization group A (plan.md:57) with `specs/mission.md` added to that group's Knowledge column. The version bump moved to task 6 and its `Cargo.toml` citations stay correct (`:85` holds `version = "0.28.1"`, `:93` holds the pinned `exasol-udf-sdk` entry at the same version). decision-log.md `[1] [plan-review]` records the direction change.

- Resolved: [TRACEABILITY_GAP] No test proved that both transport arms reach the shared function — plan.md task 1 (`:44`) adds `both_transports_call_the_shared_registration`, asserting `declare -f main | grep -c register_script_languages` equals `2` and `declare -f main | grep -c 'SET SCRIPT_LANGUAGES'` equals `0`. Verified empirically against the current `scripts/install.sh` by sourcing it and running both greps: the second count is `4` today, so the test fails before task 2 lands, exactly as the plan claims. The four matching lines are `scripts/install.sh:697`, `:698`, `:722` and `:723-725`. Two further properties were checked and hold. First, `declare -f main` strips comments, so the `ALTER SYSTEM` comment at `scripts/install.sh:607` does not inflate the count. Second, `declare -f main` renders the HTTP arm's backslash-continued `exapump sql` call as one line, so line counting matches the planned one-line call sites. The harness already sources `scripts/install.sh` (`scripts/tests/install-personal-test.sh:20`) and already runs under `set +e` (`:21`), so `main` is defined and a `grep -c` miss does not abort the run. The plan's choice of `SET SCRIPT_LANGUAGES` over `ALTER SYSTEM SET SCRIPT_LANGUAGES` is sound: it also catches a regression that reintroduces `ALTER SESSION`. The assertion is wired into both requested Scenario Coverage rows (plan.md:83 and :85), and the "single call site" overclaim is gone. plan.md:87 now reads "Two tests together cover the registration half of all three scenarios", followed by what each test pins. decision-log.md `[2] [plan-review]` records the direction change.

## Intent Fidelity

[no objection — axis checked: both interview answers still hold after the revision. Task 5 edits `specs/mission.md`, which adds no spec file and no scenario for the default path, so the "keep it spec-free" answer is intact. plan.md still carries no `## Design` section, and both decision-log entries added this round are `Promotes to ADR: no`. Task 5 is not creep: it removes a conflict this plan's own MUST creates, and round 1 raised it as a blocker. The three mission edits are surgical, matching the user's terseness instruction.]

## Feasibility

[no objection — axis checked: the two revision tasks are mechanical. Task 5 rewrites three lines in one file. Task 1's new test uses `declare -f`, which is verified to work against the sourced script in this repo's own harness. Task 2's `|| die` call sites keep the condition-context seam that suppresses `errexit` inside the callee, which is why the plan requires `return` over `exit`. The generic `die "cannot register the RUST language"` does not weaken the delta clause "it MUST fail the run, naming that the current value could not be read", because `current_script_languages` (`scripts/install.sh:498-508`) and `parse_script_languages` (`:450-474`) each print a specific stderr line naming the unreadable value. Round-1 advisories 1 and 2 (admin privilege for `ALTER SYSTEM`, and the unverified `EXA_PARAMETERS` read on SaaS and on-prem) stand unchanged at ADVISORY.]

## Requirement Quality

#### [REQUIREMENT_CONFLICT] BLOCKER
- Location: `specs/_plans/fix-install-scope-registration/container/personal-install-cloud/spec.md:44-52` against `specs/container/personal-install-cloud/spec.md:42-48`
- Issue: the delta renames the scenario it marks `DELTA:CHANGED`. The delta heading reads `### Scenario: Cloud install uses the standard HTTP transport`. The recorded heading reads `### Scenario: Cloud install uses the standard HTTP transport and scope`. `/speq:spec-merge` defines `DELTA:CHANGED` as "Replace scenario with same name", so no recorded scenario matches and the recorded one survives the merge. The permanent library then holds two scenarios in one file, the new one and the stale one, and the stale one asserts the behavior this plan removes: "it MUST register with `ALTER <scope>` honoring `--scope`, whose default is `SESSION`" (`:47`) and "it MUST NOT force `SYSTEM` scope and MUST NOT apply the read-merge-write entry preservation used by the local path" (`:48`). This is the round-1 blocker's defect class inside the feature file this plan changes. Nothing mechanical catches it: `speq plan validate fix-install-scope-registration` passes, and two differently named scenarios validate. This repo already has the convention for a renamed scenario, at `specs/_recorded/007-change-slc-runtime-debian/container/os-license-notices/spec.md:44-57`, where a rename ships as a `DELTA:REMOVED` block for the old heading plus a `DELTA:NEW` block for the new one. This delta file is unchanged since round 1, and round 1 missed the rename.
- Fix: In `specs/_plans/fix-install-scope-registration/container/personal-install-cloud/spec.md`, split the renamed scenario in two, following `specs/_recorded/007-change-slc-runtime-debian/container/os-license-notices/spec.md:44-57`. Add a `<!-- DELTA:REMOVED -->` block holding `### Scenario: Cloud install uses the standard HTTP transport and scope` with its four recorded bullets copied verbatim from `specs/container/personal-install-cloud/spec.md:42-48`. Change the marker on the current block at `:44-52` from `DELTA:CHANGED` to `DELTA:NEW`, keeping its heading and bullets as they are. Re-run `speq plan validate fix-install-scope-registration`. Change no other delta text, and add no task: the rename is a recording-time concern, not an implementation step.

## Task Breakdown

#### [COMPLETENESS_GAP] ADVISORY
- Location: `scripts/install.sh:8` and `:21-22`, against plan.md task 3
- Issue: task 3 leaves a third `--scope` prose site stale, beyond the two that round-1 advisory 4 named. The file header comment at `scripts/install.sh:8` reads "Default (a normal cluster / docker-db / SaaS): upload the tarball over the BucketFS HTTP API and register with ALTER SESSION|SYSTEM." That is the header a maintainer reads first, and after this change no path registers at `SESSION`. The same block at `:21-22` states "Registration uses ALTER SYSTEM and preserves every pre-existing SCRIPT_LANGUAGES entry" as a local-backend-only property, which becomes true of every transport.
- Fix: In plan.md task 3, add the rewrite of the header comment at `scripts/install.sh:8` to state that the default transport registers with `ALTER SYSTEM SET SCRIPT_LANGUAGES`, and the deletion of the transport-specific registration clause at `:21-22`, which the header now states once for every transport.

[no objection on the rest of the axis — checked: every delta scenario maps to a task. The personal-install deltas at `:17-19`, `:46-55` and `:64-72` map to tasks 1, 2 and 3. The personal-install-cloud deltas at `:7-9` and `:44-52` map to tasks 1 and 2. Task 5 implements no delta but removes a library conflict, and task 6 is the `CLAUDE.md` version bump named in the stated intent. Every line citation in tasks 1, 2, 3 and 6 was checked against the live files and is exact: `scripts/install.sh:69`, `:138`, `:159`, `:365`, `:435`, `:566`, `:619`, `:641-646`, `:692-698`, `:717-723`, plus `scripts/tests/install-personal-test.sh:274-283`, `:296`, `:303`, `:463-477`, `:760`, `:768`, plus `Cargo.toml:85` and `:93`. `preserves_existing_script_languages`, cited in the first Scenario Coverage row, exists at `scripts/tests/install-personal-test.sh:59`. Parallelization group A absorbs task 5 into the cluster that already owns the registration contract, and group B shares no file with it.]

## Design Depth

[no objection — axis checked: the revision adds no module, interface or boundary. `register_script_languages` still owns the read, the merge, the banner and the statement behind a two-argument interface, which is deeper than the four-step block it replaces at each call site. Round-1 advisory 5, the endpoint stated through one argument and two globals, stands unchanged at ADVISORY.]

## Prose Quality

[no objection — axis checked: Round-1 advisory 6 `[PROSE_BLOAT]` stands and now covers more text, because task 1 grew by six sentences and task 5 is a new five-sentence paragraph. Severity is unchanged at ADVISORY: every added sentence names a file, a line and a concrete edit, so no requirement became non-actionable. The added prose introduces no semicolon, contraction or em dash.]
