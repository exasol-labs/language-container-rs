[package]
name = "exasol-udf-sdk"
version.workspace = true
license.workspace = true
edition = "2021"

[dependencies]
thiserror = { workspace = true }
arrow = { workspace = true, optional = true }

[features]
connect-back = ["dep:arrow"]
