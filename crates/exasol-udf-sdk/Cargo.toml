[package]
name = "exasol-udf-sdk"
version = "0.2.1"
edition = "2021"

[dependencies]
thiserror = { workspace = true }
arrow = { workspace = true, optional = true }

[features]
connect-back = ["dep:arrow"]
