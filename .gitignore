/target/

# Serena MCP cache
/.serena/

# Built IT test-runner binary (CI artifact, produced by `cargo test --no-run`)
/it-runner

# cargo llvm-cov reports (CI artifact, produced by `cargo llvm-cov`)
/lcov-*.info

# Idea
/.idea/
