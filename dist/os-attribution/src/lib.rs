//! Intentionally empty. This crate is never compiled or linked; it exists only
//! so `cargo about` has a manifest whose `license` expression enumerates the
//! OS/runtime licenses bundled in the SLC tarball. See ../../dist/README.
